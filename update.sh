# Atualiza a lista de pacotes
    
    echo "Atualizando oa repositorios"
    sudo apt-get update

    # Atualiza todos os pacotes instalados

    #echo "Atualizando software"
    #sudo apt-get upgrade -y


    # Limpa a lista de pacotes para reduzir o tamanho da VM

   # echo "Limpando S.O"
    #sudo apt-get autoremove -y
    #sudo apt-get clean
    
    
    echo " SEU IP É: "
    ip a